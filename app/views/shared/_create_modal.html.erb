<% id ||= 'create-modal' %>
<% modal_heading ||= 'Create' %>
<% modal_subheading ||= '' %>

<%= tag.div(
      class: 'modal fade',
      id:,
      tabindex: '-1',
      'aria-labelledby': 'Create',
      'aria-hidden': 'true'
    ) do %>
  <%# local_assigns rather than a plain local: existing callers do not pass a size, and
      an undefined local would raise in their compiled templates. %>
  <div class="<%= class_names('modal-dialog', local_assigns[:modal_size]) %>">
    <div class="modal-content">
      <div class="modal-header modal-header--with-subheader">
        <h5 class="modal-title" id="create-modal-label"><%= modal_heading %></h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class='modal-subheader'>
        <p><%= modal_subheading %></p>
      </div>
      <div class="modal-body">
        <%= yield %>
      </div>
    </div>
  </div>
<% end %>
