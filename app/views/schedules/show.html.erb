<%= content_for(:title) { 'Edit Schedule' } %>
<% breadcrumb :schedule, @schedule %>

<% page_heading @schedule.subject.name_in_database %>

<% form_page_actions form_id: 'js-schedule-form', cancel_path: schedules_path %>

<div class='mb-4'></div>

<%= render 'form' %>
