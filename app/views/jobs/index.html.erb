<%= content_for(:title) { 'Jobs' } %>
<% breadcrumb :jobs %>

<% page_heading 'Jobs', 'Global view of pipeline jobs' %>

<%= render 'pipeline_jobs/jobs_table',
           jobs: @pipeline_jobs,
           total: PipelineJob.count,
           path: jobs_path,
           pipeline_scoped: false %>
