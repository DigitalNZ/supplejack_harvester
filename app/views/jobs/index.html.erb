<%= content_for(:title) { 'Jobs' } %>
<% breadcrumb :jobs %>

<%= content_for :heading do %>
  <h1>Jobs</h1>
  <p class='text-muted mb-0'>Global view of pipeline jobs</p>
<% end %>

<%= render 'pipeline_jobs/jobs_table',
           jobs: @pipeline_jobs,
           total: PipelineJob.count,
           path: jobs_path,
           pipeline_scoped: false %>
